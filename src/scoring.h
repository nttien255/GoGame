#pragma once
#include <iostream>
#include "player.h"
#include <string>

using namespace std;

void BFS_Score(int x, int y);
void calc_place_score();
void Run_Score();
string Score_Player(Player &player);
